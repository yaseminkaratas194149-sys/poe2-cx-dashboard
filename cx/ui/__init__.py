"""cx UI — a frameless Tk dashboard in the launcher / doc_nav visual language.

`theme` re-exports the launcher palette (single source of truth); `chrome` ports
the frameless-window idioms (segmented cells, single-instance, resize grip);
`cx_panel` is the two-card board + currency view; `app` is the window shell.
"""
